# TASKS.md — Master Task List / Orchestration (Primitive 7)

> Single source of truth for what exists, what's done, and what's next.
> Every task gets a row BEFORE work starts. Statuses: ⬜ todo · 🔄 in progress ·
> ✅ done (receipt logged) · ⛔ blocked.
> Sizing rule: one task = one session (< 100k tokens). 3+ files or 3+ scenarios → split.

---

## Phase F — Foundation

| ID | Task | Deps | AC (acceptance criteria) | Status | Receipt |
|---|---|---|---|---|---|
| F-1 | Session 0: decide stack + MVP domains, fill PLAN.md | — | No `⬜ DECIDE` left in PLAN.md §Stack/§Domains | ✅ | VERIFICATION.md 2026-07-16 |
| F-2 | Repo + dev stack skeleton (compose, hello-world BE+FE, healthcheck) | F-1 | `docker compose up` → BE health 200, FE renders | ⬜ | |
| F-3 | DB + migration tooling + first migration | F-2 | migrate up/down clean; schema doc started | ⬜ | |
| F-4 | Error contract + API client + CI (build+test on push) | F-2 | one endpoint returns the envelope; CI green | ⬜ | |
| F-5 | FE state & loading architecture doc (`harness/FE_STATE.md` + diagram) | F-1 | Doc covers 4 state kinds, loading/error policy, cache map, folder layout; HTML diagram renders | ✅ | VERIFICATION.md 2026-07-17 |
| F-6 | Component & alignment architecture doc (`harness/ARCHITECTURE.md` + `harness/diagrams/architecture.html`) | F-1 | Doc covers component inventory + responsibilities, layer contracts, FE↔BE and Redis interaction, alignment-enforcement gates; HTML diagram renders | ✅ | VERIFICATION.md 2026-07-17 |
| F-7 | Design system reference page (`harness/diagrams/design-system.html`) — tokens + button deep-dive + all component specimens | F-1, F-5 | Page covers tokens (color/type/spacing/radius/shadow), button anatomy/6 variants/3 sizes/5-state matrix, forms, feedback, overlays, nav, commerce patterns; HTML renders both themes | ✅ | VERIFICATION.md 2026-07-17 |
| F-8 | BE state & data architecture doc (`harness/BE_STATE.md` + diagram) — FE_STATE.md's backend counterpart | F-1, F-6 | Doc covers BE state kinds w/ owners, request data flow, transaction policy, error-code enum + mapping, validation tiers, folder layout, hard BE rules; HTML diagram renders | ✅ | VERIFICATION.md 2026-07-17 |
| F-9 | Overall build plan from `reference/docs/system/08_pages` — BE + FE + DevOps master plan (`harness/OVERALL_PLAN.md` + `harness/diagrams/overall-plan.html`) | F-1 | Plan covers all 4 role surfaces (public/customer/staff/admin), BE service/endpoint map, FE page/phase map, DevOps pipeline, phased task breakdown reconciled with existing TASKS.md phases; MD complete + HTML renders | ✅ | VERIFICATION.md 2026-07-17 |
| F-11 | BE engineering playbook from `reference/docs/be` — adopt the reusable build rules (`harness/BE_PLAYBOOK.md` + `harness/diagrams/task-F-11.html`) | F-1, F-8 | Playbook covers goose+sqlc data-layer workflow, migration-file standard, Go/Gin gotcha rules, caching discipline adds, BE build order, seed/smoke tooling rule, code-summary doc rule; zero duplication with BE_STATE/ARCHITECTURE (links instead); task page renders both themes | ✅ | VERIFICATION.md 2026-07-18 |
| F-12 | FE code-convention rules distilled from `reference/docs/fe` — extend `harness/FE_STATE.md` §9 (tokens, money format, DTO naming, derived state, method parity, role-scoped endpoints, asset URLs) + Tailwind-JIT gotcha in ENVIRONMENT.md + note F-9 cart-pivot drift in FE_STATE.md | F-5, F-9 | FE_STATE.md hard-rule list covers the 7 convention gaps with no duplication of OVERALL_PLAN §6 lessons; cart-pivot drift flagged in place; ENVIRONMENT.md carries the JIT rebuild note; docs render | ✅ | VERIFICATION.md 2026-07-18 |
| F-13 | DevOps operations doc from `reference/docs/devops` (`harness/DEVOPS.md` + `harness/diagrams/devops.html`) — ops counterpart of OVERALL_PLAN §5 | F-1, F-9 | Doc covers DevOps file ownership, compose/Dockerfile/Caddy patterns (our stack versions), CI/CD + image tagging, rollback procedures + severity/SLA, Stage A/B go-live runbook, backups, pre-deploy checklist, hard rules; zero facts duplicated from ENVIRONMENT.md (links instead); HTML renders | ✅ | VERIFICATION.md 2026-07-18 |
| F-14 | Docs alignment sweep after the owner's reference-corpus commit (`00f77d0`) — reconcile doc indexes with the repo; decision: **no folder moves** (reference paths are load-bearing) | F-9, F-11, F-12, F-13 | CONTEXT_MAP §Doc inventory has a row for every doc area in the repo (PROMPTS.md, CONTEXT_MAP itself, root README, templates/, reference/, personal/); stale "reference untracked" flag cleared from STATE.md; harness/README task summary + root README folder layout match reality; TASKS.md Phase-F rows in id order; docs render | ✅ | VERIFICATION.md 2026-07-18 |
| F-15 | Customer-menu page build plan from `reference/…/customer/customer_menu` — consolidated FE+BE plan in ONE folder (`harness/plans/customer_menu/`: `PLAN.md` + `plan.html`) reconciled with OVERALL_PLAN C/T-phase rows + FE_STATE/BE_STATE rules | F-9, F-12, F-11 | Folder holds one MD plan (FE + BE + API contract + task breakdown mapped to TASKS.md rows) and one HTML plan page rendering both themes covering FE and BE; zero rule duplication (links to owning docs); indexes updated per Hard Rule 6 | ✅ | VERIFICATION.md 2026-07-18 |
| F-16 | Canonical DB schema from `reference/docs/system/02_spec/object` — owner adopted the object-model home files as the database design → `harness/DB_SCHEMA.md` (conventions + field-name law move here from OVERALL_PLAN §3.2, per-table column specs for every spec-traced table, phase-later stubs, adopt/fix ruling on every reference §3 mismatch flag) + `harness/diagrams/task-F-16.html` | F-9, F-11 | DB_SCHEMA.md specs all 13 spec-traced tables in column detail + stubs the phase-later tables; every reference mismatch flag has an explicit adopt/fix/drop ruling linked to the owning decision doc; OVERALL_PLAN §3.2 + customer_menu plan re-pointed (no duplicated schema facts); indexes updated per Hard Rule 6 | ✅ | VERIFICATION.md 2026-07-18 |
| F-17 | Admin-overview page build plan from `reference/…/admin/admin_overview` — the 4-doc page-plan set per `plans/PAGE_PLAN_GUIDE.md` (`harness/plans/admin_overview/`: `_PLAN.md` + `_plan.html` + `_how-it-works.html` + `_mockup-1.html`) reconciled with OVERALL_PLAN O/R/AD/P rows + BE_STATE/FE_STATE/F-5 cookie-auth rules | F-9, F-15, F-16 | Folder holds the 4 slug-prefixed docs; MD is the single source of truth (each HTML footer says so); BE contract (7 endpoints incl. the consolidated SSE channel, state machine, wire shapes) + FE plan (file map, state ownership, 13 numbered behaviors) + defects-designed-out table + task mapping; zero rule duplication (links to owning docs); all 3 HTML render both themes; indexes updated per Hard Rule 6 | ✅ | VERIFICATION.md 2026-07-19 |
| F-27 | Admin-products page build plan from `reference/…/admin/admin_products` — consolidated FE+BE plan set in one folder (`harness/plans/admin_products/`: 4 docs per PAGE_PLAN_GUIDE) reconciled with DB_SCHEMA catalog tables, BE_STATE tx/error rules, FE_STATE state/cache rules, design-system F-7 neutral tokens (admin surface, NOT customer dark/orange) | F-16, F-15, F-9 | Folder holds `<slug>_PLAN.md` (FE+BE contract, write/CRUD endpoints, cache-invalidation map, behavior spec, TASKS-row mapping, defects-designed-out) + `_plan.html` + `_how-it-works.html` + `_mockup-1.html`, all rendering both themes; zero rule duplication (links owning docs); indexes updated per Hard Rule 6 | 🔄 | |
| F-28 | Admin-combos page build plan from `reference/…/admin/admin_combos` — consolidated FE+BE plan set (`harness/plans/admin_combos/`: 4 docs per PAGE_PLAN_GUIDE); combo builder consumes the products contract (cross-links F-27, does not re-derive it) | F-27 | Folder holds the 4-doc set; combo write path (combo_items array), price rule, product-picker state, and "product edited/deleted under a combo" behavior all specified; cross-links admin_products for the shared product contract; indexes updated per Hard Rule 6 | 🔄 | |
| F-24 | Customer-favourites page-plan set from `reference/…/customer/customer_favourites` — the 4-doc set per `PAGE_PLAN_GUIDE.md` in `harness/plans/customer_favourites/` (`_PLAN.md` + `_plan.html` + `_how-it-works.html` + `_mockup-1.html`) reconciled with the menu plan (shared catalog contract), FE_STATE/BE_STATE rules, and the DESIGN_PROMPT's NEW-feature proposals | F-15, F-16 | Folder holds the 4 prefixed docs; `.md` is sole source of truth (each HTML footer says so); read-only 2-GET BE contract cross-links the menu plan (no re-derive); reference's empty≡loading≡error conflation designed out with named branches; DESIGN_PROMPT NEW features (canh quick-add, live total, tự-tạo-suất, ghim-lên-menu) captured as flagged/deferred rows; both HTML themes render; indexes updated per Hard Rule 6 | ✅ | VERIFICATION.md 2026-07-19 |
| F-25 | Customer orders-&-tracking page build plan from `reference/…/customer/customer_orders_tracking` (the merged `/orders` screen = live tracking + order history + detail) — 4-doc plan set in `harness/plans/customer_orders_tracking/` per PAGE_PLAN_GUIDE.md, reconciled with the customer_menu plan (POST→/orders handoff), OVERALL_PLAN O/R rows, DB_SCHEMA order tables + FE_STATE/BE_STATE rules | F-15, F-16 | Folder holds `customer_orders_tracking_PLAN.md` (source of truth: FE+BE contract incl. SSE, behavior spec, TASKS.md row mapping, defects designed out) + `_plan.html` + `_how-it-works.html` + `_mockup-1.html`, all rendering both themes (mockup on the dark/orange shell); zero rule duplication (links to owning docs); indexes updated per Hard Rule 6 | 🔄 | |
| F-19 | Customer order-detail from `reference/…/customer/customer_order_detail` (8-doc corpus digested). **Owner ruled 2026-07-19: `/order/:id` is merged away** — detail is a view inside `/orders`, homed in the customer_orders_tracking plan. The drafted 4-doc set was therefore **discarded as ~90 % duplication** (PAGE_PLAN_GUIDE §10) and replaced by `harness/plans/customer_order_detail/customer_order_detail_SUPPLEMENT.md` owning only the three items the merge left unhomed + the Hard-Rule-5 fix to the menu plan's post-order redirect | F-15, F-16, F-9 | Supplement records the merge ruling, points at the real home, and owns: 🚨 the deep-link gap (`/orders` reachable only via client-side `activeOrderId` — shared link / 2nd phone / cleared store cannot reach a live order; `?id=` fix recommended), ⚠ the dropped quantity stepper (`PATCH /orders/items/:id/quantity`) recorded as a decision not an omission, ⚠ the menu-plan redirect drift fixed in place with dated supersession notes; zero duplication of the /orders plan; indexes updated per Hard Rule 6 | ✅ | VERIFICATION.md 2026-07-19 |
| F-26 | Admin-training page build plan from `reference/…/admin/admin_training` (staff onboarding/training-progress management surface) — 4-doc plan set per `PAGE_PLAN_GUIDE.md` in `harness/plans/admin_training/` (`_PLAN.md` + `_plan.html` + `_how-it-works.html` + `_mockup-1.html`) reconciled with DB_SCHEMA staff tables, BE_STATE tx/error rules, FE_STATE state/cache rules, and the neutral F-7 `design-system.html` tokens (admin surface, NOT the customer dark/orange shell); training status routing state machine captured; reference TRAINING_BUGS/conccern findings designed out | F-16, F-15, F-9 | Folder holds `admin_training_PLAN.md` (source of truth: FE+BE contract, endpoint table, training-status state machine, behavior spec, TASKS-row mapping, defects designed out) + `_plan.html` + `_how-it-works.html` + `_mockup-1.html`, all rendering both themes (mockup on the neutral F-7 admin shell); every reference bug mapped to a countermeasure; zero rule duplication (links owning docs); indexes updated per Hard Rule 6 | 🔄 | |
| F-20 | Admin to-do-list page build plan from `reference/…/admin/admin_todo_list` — 4-doc set per `PAGE_PLAN_GUIDE.md` in `harness/plans/admin_todo_list/` (`admin_todo_list_PLAN.md` + `_plan.html` + `_how-it-works.html` + `_mockup-1.html`) reconciled with OVERALL_PLAN AD-phase rows, DB_SCHEMA (workforce/task tables), FE_STATE/BE_STATE rules; mockup uses the **neutral F-7 `design-system.html` tokens** (admin surface, not the customer dark/orange shell) | F-9, F-16, F-7 | Folder holds `admin_todo_list_PLAN.md` (source of truth: FE+BE contract incl. assign flow + any realtime, behavior spec, TASKS.md row mapping, defects designed out) + 3 HTML companions rendering both themes; reference's empty≡loading≡error conflation designed out with named branches; every reference/TODO_BUGS finding gets a countermeasure; zero rule duplication (links owning docs); indexes updated per Hard Rule 6 | 🔄 | |
| F-21 | Admin-ingredients page build plan (`/admin/ingredients` — "Kho nguyên liệu": ingredient CRUD + Nhập/Xuất/Điều chỉnh stock-movement modal) from `reference/…/admin/admin_ingredients` — 4-doc set per `PAGE_PLAN_GUIDE.md` in `harness/plans/admin_ingredients/` (`admin_ingredients_PLAN.md` + `_plan.html` + `_how-it-works.html` + `_mockup-1.html`) reconciled with DB_SCHEMA §4.6 inventory tables + mismatch rulings 6/7/8, BE_STATE tx policy + error enum, FE_STATE admin-CRUD rules; mockup on the **neutral F-7 `design-system.html` tokens** (admin surface, not the customer dark/orange shell) | F-16, F-9, F-7 | Folder holds `admin_ingredients_PLAN.md` (source of truth: 8-endpoint BE contract w/ manager+/admin-only DELETE RBAC split, wire shapes using DB names per ruling 6, status-derivation table, behavior spec, TASKS.md row mapping, reference defects designed out — non-tx stock update→locked tx, missing initial `in` movement, out-below-zero silent clamp→VALIDATION_FAILED, 409-dup/422-in-use gaps) + `_plan.html` + `_how-it-works.html` + `_mockup-1.html`, all rendering both themes on F-7 tokens; zero rule duplication (links owning docs); indexes updated per Hard Rule 6 | 🔄 | |
| F-22 | Admin task-board page build plan (`/admin/staff/task-board` — per-staff daily task dashboard: date filter, 4 KPIs, staff table with lazy row-expand, assign-task modal) from `reference/…/admin/admin_task_board` — 4-doc set per `PAGE_PLAN_GUIDE.md` in `harness/plans/admin_task_board/` (`admin_task_board_PLAN.md` + `_plan.html` + `_how-it-works.html` + `_mockup-1.html`); promotes the `staff_tasks` table out of `DB_SCHEMA.md §4.7` stub; cross-links the sibling `admin_todo_list` plan (same table + same 2 read endpoints — no re-derive) | F-15, F-16, F-9 | Folder holds the MD plan (FE+BE contract, behavior spec, TASKS-row mapping) + 3 HTML companions rendering both themes on F-7 neutral tokens (admin surface, not the customer dark/orange shell); **all 4 reference bugs designed out** — write-once `pending` status → status-transition endpoint + read-side overdue derivation, fabricated `qualityScore` dropped, FK-reject 500 → 422 `VALIDATION_FAILED`, dead status filter → per-status counts in the stats DTO; invalidate-on-`task.dueDate`-vs-`filters.date` miss designed out; `staff_tasks` columns promoted into DB_SCHEMA.md; zero rule duplication (links owning docs); indexes updated per Hard Rule 6 | 🔄 | |
| F-23 | Admin-staff page build plan from `reference/…/admin/admin_staff` — 4-doc set per `PAGE_PLAN_GUIDE.md` in `harness/plans/admin_staff/` (`admin_staff_PLAN.md` + `_plan.html` + `_how-it-works.html` + `_mockup-1.html`) reconciled with DB_SCHEMA §4.4 `staff` (5-value role enum + soft-delete×UNIQUE rename rule), BE_STATE §4 9-code error enum, BE_PLAYBOOK sqlc pipeline, FE_STATE URL-owned filter rule; mockup uses the **neutral F-7 `design-system.html` tokens** (admin surface, not the customer dark/orange shell) | F-16, F-9, F-7 | Folder holds `admin_staff_PLAN.md` (source of truth: 6-endpoint FE+BE contract, RBAC hierarchy guards, behavior spec, TASKS-row mapping, defects designed out) + 3 HTML companions rendering both themes; every reference flag (dead self-guard, unused filter params, 100-row cap, no-error-branch drawer, loading≡empty≡no-match conflation, unrevoked refresh tokens, `performance_score` stub) gets an explicit adopt/fix/drop ruling; reference error codes mapped onto our 9-code enum; zero rule duplication (links owning docs); indexes updated per Hard Rule 6 | 🔄 | |

## Phase 1 — Catalog

| ID | Task | Deps | AC | Status | Receipt |
|---|---|---|---|---|---|
| C-1 | Catalog schema: products + categories migration + SQL seed script | F-3 | migrate up/down clean; seed inserts ≥ 10 products across ≥ 3 categories | ⬜ | |
| C-2 | BE: product list (paging + category filter) + product detail endpoints | C-1, F-4 | curl transcripts: list page 2, filtered list, detail, 404 in error envelope | ⬜ | |
| C-3 | BE: category list + product search (LIKE-based v1) | C-2 | curl: categories; search hits + empty result | ⬜ | |
| C-4 | FE: product list page (grid, category filter, paging) | C-2 | screenshot: grid renders seeded products; filter works | ⬜ | |
| C-5 | FE: product detail page | C-4 | screenshot: detail renders name/price/stock from API | ⬜ | |

## Phase 2 — Cart & Checkout

| ID | Task | Deps | AC | Status | Receipt |
|---|---|---|---|---|---|
| CC-1 | Cart schema (guest cart via cookie token) migration | C-1 | migrate up/down clean; token uniqueness enforced | ⬜ | |
| CC-2 | BE: cart endpoints (get / add / update qty / remove) | CC-1 | curl: full add→update→remove round-trip; stock-cap rejection in envelope | ⬜ | |
| CC-3 | FE: cart page + add-to-cart from product pages | CC-2, C-5 | screenshot: badge count updates; cart page edits quantities | ⬜ | |
| CC-4 | BE: checkout — address + order placement (price snapshot, atomic stock decrement) | CC-2, O-1 | curl: place order → order id; stock decremented; insufficient-stock rejection | ⬜ | |
| CC-5 | FE: checkout flow (address form → place order → confirmation) | CC-4 | screenshot: order placed from UI, confirmation shows order id | ⬜ | |

## Phase 3 — Orders (Payment ⏸ deferred)

| ID | Task | Deps | AC | Status | Receipt |
|---|---|---|---|---|---|
| O-1 | Orders schema: orders + order_lines + status enum migration | C-1 | migrate up/down clean; lifecycle enum matches PLAN.md §Business rules | ⬜ | |
| O-2 | BE: my-orders list + order detail + cancel (window per rule 2, stock restore) | O-1, CC-4 | curl: list, detail, cancel `placed` OK, cancel `shipped` rejected in envelope | ⬜ | |
| O-3 | FE: merged `/orders` screen — **static half** (history list from `ORDER_CACHE`, active-order summary + rollup, inline detail cards, the two cancels) per `plans/customer_orders_tracking/…_PLAN.md` §4. ⚠ **Split from the live half** (→ R-2): this row was one row for two ACs; live surfaces are blocked on R-1 | O-2 | screenshots: history lists cached orders; summary shows the Bàn 04 rollup (7 loại); detail shows ra/còn per dish; cancel round-trip incl. a `422 CANCEL_THRESHOLD` refusal | ⬜ | |
| — | Payment gateway integration | ⏸ | deferred Session 0 — no gateway in v1 (COD); re-scope when phase opens | ⛔ | |

## Phase R — Realtime (registered by F-25; design in `OVERALL_PLAN.md §3.5`)

| ID | Task | Deps | AC | Status | Receipt |
|---|---|---|---|---|---|
| R-1 | BE: realtime platform pkg + **the one event contract** (one Go file / one TS file) + monitor SSE `GET /sse/order-monitor/:id` — snapshot-on-connect, own-order/table ownership gate, **BE-computed queue `position` + `estimated_minutes`**, separate pub/sub Redis client, 15 s keep-alive + `retry:` | O-2 | two-client SSE transcript (chef `qty_served++` → customer stream receives `item_progress`, queue advances); reconnect transcript proving a status change during the drop is replayed by the snapshot; `403` proving the ownership gate | ⬜ | |
| R-2 | FE: `/orders` **live half** — `useOrderMonitorSSE` with the exhaustive event switch (CI fails on an unhandled published type), connection pill + banner, live status badge, queue position/ETA banner, per-dish live progress, whole-floor queue, status-notification modal | R-1, O-3 | two-client screencap: chef serves → this screen's dish row + progress bar + floor queue move with no refresh; SSE-down branch stays usable | ⬜ | |

## Phase 4 — Accounts (Admin · AI assistant ⏸ deferred)

| ID | Task | Deps | AC | Status | Receipt |
|---|---|---|---|---|---|
| A-1 | Users schema + BE register/login (bcrypt, JWT) + auth middleware | F-3 | curl: register, login → JWT, protected route 401 without / 200 with token | ⬜ | |
| A-2 | FE: register/login pages + session handling in the API client | A-1 | screenshot: login → logged-in header state; token attached to API calls | ⬜ | |
| A-3 | Cart merge on login + orders tied to account (order history per user) | A-1, CC-2, O-2 | curl: guest cart merges per PLAN.md rule 5; my-orders scoped to the user | ⬜ | |
| — | Admin (product CRUD, order mgmt, dashboard) | ⏸ | deferred Session 0 — v1 manages products via SQL scripts | ⛔ | |
| — | AI assistant (shopping chat) | ⏸ | deferred Session 0 | ⛔ | |

---

## Rules

1. New work with no row here → STOP, register first (`templates/TASK_TEMPLATE.md`),
   confirm with owner, then start.
2. ✅ requires: AC demonstrated + receipt row in `VERIFICATION.md` + `STATE.md` updated.
3. A blocked task records WHY in its row and gets a flag in the session summary.
