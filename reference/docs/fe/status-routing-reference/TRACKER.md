# /status-routing-reference Progress Tracker

> One source of truth for all `/status-routing-reference` runs.
> A Status Routing Reference = the code-accurate table mapping every entity status to the
> zone/component that renders it (+ action buttons + per-zone rules).
> Update status + concerns after each run. Never leave a row blank after a session.
> Model file: `docs/fe/wireframes/admin_main/admin_overview/Admin_Overview_Status_Routing_Reference.md`

---

## Status Legend

| Symbol | Meaning |
|--------|---------|
| ⬜ | Not started |
| 🔄 | In progress |
| ✅ | Done — every cell traced to code |
| ⚠️ | Done but has `❓ UNVERIFIED` cells / open concerns |
| ❌ | Blocked / page not coded yet |
| N/A | No entity statuses — a routing reference does not apply |

---

## Client Pages

| # | Page | Command | Status | Last Run | Concerns / Notes |
|---|------|---------|--------|----------|-----------------|
| C1 | Menu | `/status-routing-reference client_menu_page` | N/A | 2026-06-07 | No entity status routing (catalog only). Concerns: (1) banner.jpg 404; (2) Canh price 0₫; (3) 🚨 toppings unselectable from menu list (`ProductCard hasToppings=false`); (4) 🚨 toppings never shown in "Tóm tắt đơn hàng" (OrderSummary ignores `item.toppings`) — active, seed gives every bánh nhân toppings; (5) ⚠️ menu card uses `filling` field but DB models nhân/rau as toppings → 2 add paths disagree. |
| C2 | Product Detail | `/status-routing-reference client_product_detail` | N/A | — | No entity status routing |
| C3 | Order | `/status-routing-reference client_order_page` | ⚠️ | 2026-06-07 | Refreshed to new skill shape (Live Snapshot + FROM/TO/CROSS-PAGE headings). All code cells traced. Live snapshot NOT captured (Playwright profile locked + needs auth'd real order). ⚠️ `paid` treated as active. ⚠️ pending can't whole-order cancel. ⚠️ dead SSE branches `order_init`/`order_completed` never emitted by BE. Fixed: FE OrderItem has `flagged`, not `item_status`. |
| C4 | Monitoring / Servicing Table | `/status-routing-reference client_tracking` | ⚠️ | 2026-06-07 | Page = `app/(shop)/tracking`. Read-only: status gates zones, no status buttons. 1 GET (`['order',id]`) + 1 SSE (3 channels) + 0 writes. Entered from C3 "Theo dõi đơn". 🚨 C1: FE listens SSE `order.status` but BE emits `order_status_changed` → Zone B badge never advances live. 🚨 C2: "BÀN BẠN ★" highlight broken (table_id UUID vs cell id=name). ⚠️ dead events (`order_cancelled` ignored), dead per-row ETA, auth uses accessToken not spec guestToken. Live snapshot NOT captured (Playwright profile locked). |
| C5 | Favourites | `/status-routing-reference client_favourite_page` | N/A | — | No entity status routing |
| C6 | Info | `/status-routing-reference client_info_page` | N/A | — | No entity status routing |

---

## Admin Pages

| # | Page | Command | Status | Last Run | Concerns / Notes |
|---|------|---------|--------|----------|-----------------|
| A1 | Products | `/status-routing-reference admin_main/admin_main_product` | N/A | — | availability flag only, not a status flow |
| A2 | Categories | `/status-routing-reference admin_main/admin_main_categories` | N/A | — | No entity status routing |
| A3 | Toppings | `/status-routing-reference admin_main/admin_main_topping` | N/A | — | availability flag only |
| A4 | Combos | `/status-routing-reference admin_main/admin_main_combos` | N/A | — | No entity status routing |
| A5 | Staff | `/status-routing-reference admin_main/admin_main_staff` | ✅ | 2026-06-07 | Status-routed but boolean (`staff.is_active` active/inactive), not enum + no SSE. Toggle pill drives PATCH /staff/:id/status. Concerns: (1) ⚠️ deactivate has NO confirm but re-activate does (asymmetric, risky direction unguarded); (2) StatsBar counts global vs table shows filtered slice; (3) ⚠️ listStaff hardcodes ?limit=100, ignores meta.total. Live snapshot NOT captured (Playwright profile locked, stack was up). |
| A6 | Marketing | `/status-routing-reference admin_main/admin_main_marketing` | N/A | — | No entity status routing |
| A7 | Storage | `/status-routing-reference admin_main/admin_main_storage` | ⚠️ | 2026-06-07 | Page = `/admin/ingredients`. Real status enum = `in_stock·low_stock·expiring_soon·out_of_stock` (4 vals, **derived server-side** in `ingredient_handler.go:14-26`, not stored, not button-set → no status-advance flow). Corrected tracker's wrong `ok/expiring_soon/expired` guess. All code cells traced; 1 ❓ UNVERIFIED (BE list order). Live snapshot NOT captured (Playwright profile locked + manager auth needed). Concerns: (1) 🟠 StockMoveModal unreachable (`setModal('move')` never fired) → qty-driven statuses frozen post-create; (2) 🟠 delete shown to managers but BE = admin-only; (3) 🟡 red-qty (FE `qty<=threshold`) vs `low_stock` badge can disagree. |
| A8 | Todo List | `/status-routing-reference admin_main/admin_main_todo_list` | ✅ | 2026-06-07 | staff_tasks status × zone (C stats board / D task list). Page READS statuses only — no status-advance button, no update/delete endpoint. Concerns: (1) ⚠️ status filter dropdown dead (never passed to query); (2) 🚨 edit mode calls createTask → duplicates (no updateTask API/route); (3) ⚠️ no admin path to advance status; (4) ⚠️ end_date ignored (single-date query); (5) page+qualityScore unused. Live snapshot not captured (Playwright profile locked + admin auth). |
| A9 | Staff Task Board | `/status-routing-reference admin_main/admin_main_staff_task_boad` | ✅ | 2026-06-07 | Page `/admin/staff/task-board`. NOT a status-column board — rows are STAFF (perf grid), task status only in KPI cards (Zone D) + lazy per-staff expanded sub-list (Zone F badges). No status-mutating buttons — only write is POST create-task (defaults `pending`). Concerns: (1) 🚨 status filter dead except `overdue`; (2) ⚠️ "board" has no columns; (3) ⚠️ no nav link (nav "Công việc" → todo-list); (4) ⚠️ 2 create paths (inline useMutation vs useCreateTask). 1 GET stats + 1 GET tasks + 1 POST. Live snapshot CAPTURED (admin auth). All cells traced. |
| A10 | Training | `/status-routing-reference admin_main/admin_main_training` | ⚠️ | 2026-06-07 | No stored status enum — completion status (Hoàn thành/Đang học/Chưa qua quiz/Chưa bắt đầu) is **derived** client-side from `watched_percent` + latest `quiz_attempts.passed`. Concerns: 🟡 icon conflates failed-quiz vs watching; dead `TrainingStatus` type + dead `useTrainingStore`; 🚨 unwrap inconsistency (`listGuideProgress` → `r.data`, siblings → `r.data.data`); blank staffName to Modal 2; mutations have no error UI. Live snapshot NOT captured (Playwright profile locked). |
| A11 | Overview | `/status-routing-reference admin_main/admin_overview` | ✅ | 2026-06-05 | Model reference. order status × zone (B/C/D/E/F) + table.status + action buttons + per-zone rules. |
| A12 | Summary | `/status-routing-reference admin_main/admin_summary` | N/A | 2026-06-07 | Confirmed N/A — analytics/KPI dashboard, no entity-status routing in JSX. Wrote data-flow reference (5 zones · 4 GET reads · 1 POST stock-in · no Zustand/localStorage). Added BE-side "Order/Payment Status → KPI Mapping" (page's real status logic lives in analytics_repo.go). Concerns: (1) `dishes_sold` label says delivered but BE counts delivered+paid; (2) `active_tables` ignores range (live); (3) StockAlertList derives 🔴/🟡 from quantity<warningThreshold, ignores Ingredient.status enum. ❓1 unverified: low-stock BE sort order. |
| A13 | KDS (Kitchen) | `/status-routing-reference kds` | ⚠️ | 2026-06-07 | All cells traced. NOT a per-status column board — one card grid gated by `ACTIVE_STATUSES`={pending,confirmed,preparing}. 🚨 item-serve route `PATCH /orders/:id/items/:id/status` does NOT exist (404; real=`PATCH /orders/items/:id` `{qty_served}`). 🚨 `✓ Phục vụ` & `🛍 Mang đi` both send `status:ready` (dupe). ⚠️ label drift (own statusLabel: "Đang chuẩn bị"/"Đã phục vụ" ≠ StatusBadge). ⚠️ jumps active→ready/cancelled, no step-ladder. Live snapshot not captured (browser locked + auth-gated). Doc: `docs/fe/wireframes/admin_kds/KDS_Status_Routing_Reference.md`. |
| A14 | POS / Payment | `/status-routing-reference pos` | ⚠️ | 2026-06-07 | Wrote `Pos_Payment_Status_Routing_Reference.md` (spans `/pos` + `/cashier/payment/[id]`). All code cells traced; WS events verified in BE (`order_status_changed`, `payment_success`). Live snapshot NOT captured (Playwright MCP unavailable + both routes CASHIER-gated). 🚨 COD broken: FE sends `method:'cod'`, BE binding `oneof=vnpay momo zalopay cash`. ⚠️ POS bypasses `order-payload.ts` (inline `items[]`, no filling/combo/topping). ⚠️ cancelled active order leaves waiting card stuck; `failed` payment is a dead-end. |

---

## Cross-Page Concerns

> Things that affect multiple references — shared status enums, label drift, transition rules.

| # | Concern | Affects Pages | Status | Notes |
|---|---------|--------------|--------|-------|
| X1 | Order status enum is single-source — every reference must use the exact same 7 values | C3, C4, A11, A13, A14 | ✅ | Source: `docs/be/be_code_summary/DB_SCHEMA_SUMMARY.md` (`orders.status`). pending·confirmed·preparing·ready·delivered·cancelled·paid |
| X2 | Vietnamese status labels must match across all references (no label drift) | C3, C4, A11, A13, A14 | ⚠️ | A11 + C3 use identical labels from `StatusBadge.tsx` (single source). **A13/KDS DRIFTS** — it has its OWN `statusLabel` map (page.tsx:51-61): `preparing`="Đang chuẩn bị" (badge="Đang làm"), `delivered`="Đã phục vụ" (badge="Đã giao"), `paid` missing (renders raw). KDS should use StatusBadge. `ready`="Sẵn sàng" matches. |
| X3 | Status transition rules (which button → which next status) consistent BE vs FE | A11, A13, A14 | ⚠️ | A11 = full ladder (pending→confirmed→preparing→ready→delivered). C3 = customer cancel/qty only. **A13/KDS does NOT use the ladder** — it jumps any active order straight to `ready` (`✓ Phục vụ` & `🛍 Mang đi`, both identical) or `cancelled` (`Huỷ`); a `pending` order can be marked `ready` skipping confirmed/preparing. Confirm intended vs Admin Overview. A14 pending. |
| X4 | `isActive` semantics — `paid` counts as active on C3 (excludes only delivered/cancelled) | C3, C4, A13, A14 | ⚠️ | Found in C3 (`order/[id]/page.tsx:232`). Confirm whether `paid` should disable edit/cancel/add on all client+staff order surfaces. A14/Payment never re-reads order status after pay (treats `completed` payment as terminal), so not surfaced there. |
| X6 | Payment method enum mismatch — FE sends `cod`, BE binding requires `cash` | A14 | 🚨 | FE `PaymentMethod`=`vnpay\|momo\|zalopay\|cod` + default `cod` (`cashier/payment/[id]/page.tsx:14,52`); BE binding `oneof=vnpay momo zalopay cash` (`payment_handler.go:25`). The in-store cash path 400s. Fix FE to `cash`. |
| X5 | `staff_tasks.status` enum is single-source — A8 + A9 must use identical 4 values + VN labels | A8, A9 | 🔄 | Source: `DB_SCHEMA_SUMMARY.md:356` (`pending·in_progress·completed·overdue`), labels from `TaskStatusBadge.tsx` (Chờ·Đang làm·Hoàn thành·Quá hạn). Separate from order enum (X1). **Both A8 + A9 confirmed read-only for status — NEITHER has a status-transition button** (the presumed "transitions live in A9" was wrong). A9 only creates tasks (defaults `pending`); status advance has no FE surface in either page. Same `TaskStatusBadge` single source — no label drift. |
| X7 | `ingredient.status` is derived server-side (4-value enum `in_stock/low_stock/expiring_soon/out_of_stock`, `ingredient_handler.go:14-26`) but FE surfaces re-derive a "low" signal from `quantity <= warningThreshold` and ignore the enum | A7, A12 | ⚠️ | A7 (Storage table) shows red quantity text via FE `qtyClass` independent of the badge; A12 (Summary StockAlertList) computes 🔴/🟡 from `quantity<warningThreshold` and never reads `Ingredient.status`. Both can disagree with the BE enum (qty 0 → `out_of_stock` badge + red qty; expiring item with healthy qty → `expiring_soon` but no red qty). Single-source the badge; don't re-derive on FE. |

---

## Session Log

> One row per `/status-routing-reference` session. Append, never edit old rows.

| Date | Page | Outcome | Key decisions / notes |
|------|------|---------|------------------------|
| 2026-06-05 | admin_main/admin_overview | ✅ | Seeded as the model reference (pre-existing file renamed from table_status.md → Admin_Overview_Status_Routing_Reference.md). All zones, statuses, buttons, and per-zone rules captured. |
| 2026-06-05 | client_order_page | ⚠️ | Tracking page `order/[id]`. Reframed matrix: single order, status gates buttons/banners/modals (not zone routing). Found 2 BE inconsistencies: `paid` is active (X4), `pending` can't whole-order cancel. Notification modal = SSE-transition-driven, separate from stored status. |
| 2026-06-07 | client_menu_page | N/A | Confirmed N/A (catalog + cart, no entity-status read). Wrote `Menu_Status_Routing_Reference.md` reframed as a data-flow reference (zones · 4 GET queries · single POST /orders write · cross-page stores · OrderSummary rules) instead of a status matrix. 2 concerns logged: banner.jpg 404, Canh price 0₫. |
| 2026-06-07 | client_order_page | ⚠️ | Refresh to new skill structure. Added Live Page Snapshot (couldn't capture — browser profile locked + page needs auth'd real order id; documented why). Restructured Data Management into the 3 canonical headings (FROM BE reads / TO BE writes / CROSS-PAGE) + DishRow preview-math breakdown. Code re-verified against current `[id]/page.tsx` + `useOrderSSE.ts`. Accuracy fix: FE `OrderItem` carries `flagged`, not `item_status` (old §1 was wrong). 3 standing flags unchanged (paid-active, pending no whole-cancel, dead `order_init`/`order_completed` SSE branches). |
| 2026-06-07 | admin_main/admin_main_staff | ✅ | First admin page done. Status is a **boolean** (`staff.is_active` → active/inactive), not the order-status enum — reframed matrix around the 2 states. Toggle pill (StaffTable) is the sole status write → `PATCH /staff/:id/status {is_active}`; no SSE, no optimistic update, client-side filter routes which state shows in the table while StatsBar counts globally. 4 mutations documented (create/edit/status/delete) with literal bodies + error-code→toast maps (USERNAME_TAKEN/SELF_DEACTIVATION_FORBIDDEN/LAST_ADMIN). Cross-page = auth store only (user.id/role for self-toggle + canDelete hierarchy); no localStorage handoff. All cells code-traced; live snapshot skipped (browser profile locked). |
| 2026-06-07 | client_tracking | ⚠️ | Page = `app/(shop)/tracking` (the empty `tracking/[id]/` folder is unused; orderId comes from `cart.activeOrderId`). Read-only monitor: order status gates zones (B banner copy, D queue row membership, E table colour) but there are **zero** status-changing buttons and **zero** writes. Wrote full reference: 6 zones, 7-status matrix, 1 GET + 1 SSE (3 Redis channels) + 0 POST, cross-page = cart.activeOrderId + auth.accessToken. **2 🚨 bugs found:** (C1) FE SSE listens `order.status` but BE publishes `order_status_changed` → Zone B status badge never advances live (Zone D does, via queue.update → same order shows 2 statuses); (C2) "BÀN BẠN ★" table highlight broken — compares order.table_id (UUID) to cell id set to table **name**. Plus dead ignored events (`order_cancelled`/`new_order`/`item_progress`), dead per-row queue ETA, auth via accessToken not spec's guestToken. Live snapshot NOT captured (Playwright profile locked). |
| 2026-06-07 | admin_main/admin_summary | N/A | Analytics dashboard — confirmed no FE entity-status routing. Wrote data-flow reference. Key finding: the page's only "status routing" is BE aggregation (which order/payment statuses feed each KPI), traced to analytics_repo.go and captured as a Status→KPI table (customers != cancelled; dishes_sold & top-dishes & staff-perf = delivered+paid; revenue & staff-revenue = payments.completed; active_tables = confirmed/preparing/ready and NOT range-filtered). Live snapshot skipped (Playwright profile locked + admin-gated). 3 concerns: delivered+paid label drift, active_tables range-independent, stock alert ignores Ingredient.status enum. |
| 2026-06-07 | admin_main/admin_main_todo_list | ✅ | `staff_tasks` status routing. Page is **read-only for status** — it displays the 4-value enum (Zone C stats board when no staff picked / Zone D task list when staff picked) but has NO status-advance button and BE exposes only 3 routes (stats/list/create), no update/delete. Built full matrix (status × zone), data flow (3 GET reads + 1 POST create), cross-page (auth store only, no localStorage handoff, stats-row click swaps C→D in-page). 5 concerns: 🚨 edit mode calls createTask (duplicates, no updateTask); ⚠️ status filter dropdown dead (never passed to query); ⚠️ no admin path to advance status; ⚠️ end_date ignored (single-date); page+qualityScore unused. Added cross-page concern X5 (shared task enum w/ A9). Live snapshot blocked (Playwright profile locked, same as prior runs). | Completion status is **derived** in `CompletionTrackingTable` from `watchedPercent` + `quizPassed (bool|null)` → 4 VN labels (Hoàn thành/Chưa qua quiz/Đang học/Chưa bắt đầu); `published` is a separate draft/published flag. Documented derived-status matrix + 3-step modal derivation + full data flow (3 GET reads, 4 writes incl. debounced notes PATCH, no SSE, no cross-page state). 6 concerns: 🟡 icon conflates failed vs watching; dead `TrainingStatus` type; dead `useTrainingStore`; unwrap mismatch on `listGuideProgress`; blank staffName→Modal 2; no mutation error UI. Live snapshot blocked (Playwright profile locked, same as client_order_page). |
| 2026-06-07 | client_menu_page | N/A | Topping audit (selection → cart → "Tóm tắt đơn hàng"). Verdict: filling + combo items + canh/rau correct; toppings WRONG. Seed confirms nhân thịt/mộc nhĩ + rau mùi tàu are DB toppings on every bánh → gap is active. 3 concerns added (3/4/5): menu list can't pick toppings (hasToppings=false), OrderSummary never renders item.toppings, menu-card `filling` vs detail-page topping = 2 disagreeing add paths. Logic fix proposed, awaiting owner align (MASTER row + confirm before coding). |
| 2026-06-07 | admin_main/admin_main_staff_task_boad | ✅ | Page `/admin/staff/task-board`. Same `staff_tasks` enum as A8 (X5) but a **different surface**: rows are STAFF (perf grid), not tasks → no Kanban/status-column routing. Task status surfaces only in (a) 4 KPI cards (Zone D, from `metrics`) and (b) the lazy per-staff expanded sub-list (Zone F, `TaskStatusBadge`). No status-mutating buttons — only write is `POST /admin/tasks` (create, defaults `pending`, no status field). Live snapshot CAPTURED (logged in admin/Admin@123; 5 seed staff rows all-0, expand → empty Zone F). All cells code-traced. 5 concerns: 🚨 status filter dead except `overdue`; ⚠️ "board" has no columns; ⚠️ no nav link (nav→todo-list); ⚠️ 2 create paths (inline useMutation vs useCreateTask); benign /auth/me 401. |
| 2026-06-07 | admin_main/admin_main_storage | ⚠️ | Wrote `Storage_Status_Routing_Reference.md` for `/admin/ingredients`. Status IS routed but **derived server-side** (4-value enum `in_stock/low_stock/expiring_soon/out_of_stock` in `ingredient_handler.go:14-26`, not stored, no status-advance buttons) — corrected the tracker's wrong `ok/expiring_soon/expired` guess. 4 writes mapped (create/update/delete/stock-movement) with literal bodies + error->toast (409 dup, 422 in-use). RBAC: manager for CRUD, delete=admin-only. No cross-page state (pure TanStack Query, no Zustand/localStorage). 3 concerns: dead StockMoveModal (`setModal('move')` never fired -> qty-driven statuses frozen post-create), delete RBAC mismatch (FE shows Xoa to managers), dual low-signal divergence (FE red-qty vs BE low_stock badge - relates to A12 stock-alert concern). 1 UNVERIFIED: BE list ordering. Live snapshot blocked (profile lock + manager auth). |
| 2026-06-07 | pos | ⚠️ | A14. POS flow spans **2 routes**: `(dashboard)/pos` (browse→cart→create→wait) + `(dashboard)/cashier/payment/[id]` (receipt→method→pay→print→/pos). POS has NO status chip — order status drives side-effects only (gate-on-local-`activeOrder`; WS `order_status_changed`+`status==='ready'`→auto-push to Payment; refetches `GET /orders/:id` imperatively, trusts fetched status). Payment renders payment status (E method-picker / F QR). Both WS events verified in BE (`order_status_changed` order_service.go:543,736; `payment_success` payment_service.go:270). Reads: POS=categories+products(+imperative order); Payment=`['order',id]`. Writes: POST /orders (source=pos), POST /payments, PATCH /payments/:id/proof. Cross-page=auth-store token + URL orderId only; **POS cart is local `useState`, NOT shared cart.ts, unpersisted**. **6 concerns:** 🚨 COD broken (FE `cod` vs BE `cash` binding — added X6); ⚠️ POS bypasses `order-payload.ts` (inline items[], no filling/combo/topping); ⚠️ cancelled active order → waiting card stuck; ⚠️ `failed` payment = dead-end (no E/F branch); 💡 receipt shows raw table_id UUID; 💡 cart unpersisted. Live snapshot NOT captured (Playwright MCP only became available post-write; both routes CASHIER-gated). 0 unverified cells. Doc: `docs/fe/wireframes/admin_pos/Pos_Payment_Status_Routing_Reference.md`. |
| 2026-06-07 | kds | ⚠️ | A13. KDS = `(dashboard)/kds`. NOT a per-status column board (tracker note was wrong) — one responsive card grid gated by `ACTIVE_STATUSES`={pending,confirmed,preparing}; ready/delivered/cancelled/paid never render (filtered on load, removed live on WS `order_status_changed`/`order_cancelled`). Reads: 1 GET `/orders` (staleTime 30s) + 4 WS msg types over shared `/ws/orders-live` (new_order/item_progress/order_cancelled/order_status_changed — all 4 confirmed emitted by order_service.go). Writes: `PATCH /orders/:id/status` (ready/cancelled). Cross-page = auth store token only, no localStorage/cart. **2 🚨 bugs found:** (1) item-row click → `PATCH /orders/:id/items/:id/status {}` — route does NOT exist (real=`PATCH /orders/items/:id {qty_served}`) → 404; item progress only updates via WS today; (2) `✓ Phục vụ` & `🛍 Mang đi` send identical `status:ready`. Updated X2 (KDS label drift) + X3 (KDS skips ladder). Live snapshot not captured (browser profile locked + auth-gated). 0 unverified cells. |

---

## Quick-Run Order (Recommended)

Run the status-heavy pages first — they share the order status enum and surface label/transition drift early:

```
1. /status-routing-reference admin_main/admin_overview   ← model (done)
2. /status-routing-reference kds                          ← order status → column
3. /status-routing-reference pos                          ← order + payment status
4. /status-routing-reference client_order_page
5. /status-routing-reference client_tracking
6. /status-routing-reference admin_main/admin_main_storage
7. /status-routing-reference admin_main/admin_main_todo_list
8. /status-routing-reference admin_main/admin_main_staff_task_boad
9. /status-routing-reference admin_main/admin_main_training
```
